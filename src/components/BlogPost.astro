---
import type { BlogPost } from "../../utils/types";

type Props = BlogPost;

const { publishDate, title }: Props = Astro.props;
---

<main>
  <article>
    <p>{publishDate}</p>
    <h1>{title}</h1>
    <slot />
  </article>
</main>
